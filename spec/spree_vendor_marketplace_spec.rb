require "spec_helper"

describe SpreeVendorMarketplace do
  it "has a version number" do
    expect(SpreeVendorMarketplace::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
